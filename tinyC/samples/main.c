int main() {
    int isaula;
    int kevin;
    int raim;
    char test = (char)raim;

    int x = division(12, 4);

    if(x)
        isaula = "puto";
    else
        isaula = makePuto(kevin);

    for( i = 0; i < 100; i++)
        isaula += reverse("otup");

    while(isaula == "puto") {
        kevin = x % 2 == 0 ? 1 : "celoso";
        raim = x % 2 == 0 ? 2 : "pareja de isaula\n"; 
    }
    return 0;
}